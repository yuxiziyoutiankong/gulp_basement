# gulp_basement
this is a basement file

git index & git stage & git commit & git workspace 

git checkout -b dev , git branch dev & git checkout dev;
git status & git status -s & git add . | git add --all 
&git reset head [fileName] & git check -- [fileName]
&git commit 
git reset --hard "",git reset --hard head~0,1,2,3
git log & git reflog & git rm [fileName] | git commit -m "" & git checkout -- [fileName]
git checkout -b dev & git branch dev | git checkout dev 
& git branch 
git remote add origin "1124@ad.com" & git remote add [] or remove []
git bash:ssh-keygen -t rsa -C "234@aa.com"
git push -u origin master --force & git pull origin master:master 
git clone "asdfa.com/eers;sfdxxx" "support many protocol including ssh http(s) git "  & git clone [] [localFileName]
git pull = git fetch & git merge or git rebase 
& git fetch origin master [remoteName] [branchNameOfRemote]
git branch -r & git branch -a 
git checkout -b dev origin/master "at remote master branch built a dev branch"
git branch --set-upstream master origin/next

$ git config --global push.default matching
# Лђеп
$ git config --global push.default simple

&git stash list & git stash apply stash@{} | git stash drop stash@{}
&git stash pop stash@{}


